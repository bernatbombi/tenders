meta {
  name: Analyse tender
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/tenders/:slug/analyze
  body: none
  auth: bearer
}

params:path {
  slug: {{slug}}
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  const data = res.getBody();
  bru.setVar("jobId", data.jobId);
}
