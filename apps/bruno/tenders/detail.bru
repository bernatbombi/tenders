meta {
  name: Fetch tender detail
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/tenders/{{expediente}}/detail
  body: none
  auth: bearer
}

params:path {
  expediente: 
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  const data = res.getBody();
  bru.setVar("jobId", data.jobId);
}
