meta {
  name: Fetch tender detail
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/tenders/:slug/detail
  body: none
  auth: bearer
}

params:path {
  slug: {{slug}}
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  const data = res.getBody();
  bru.setVar("jobId", data.jobId);
}
